function solve(inputStr, startingIndex, count) {
    let result = inputStr.substring(startingIndex, startingIndex + count);
    
    console.log(result)
}

// solve('ASentence', 1, 8);
// solve('SkipWord', 4, 7);